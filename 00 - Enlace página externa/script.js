// Este es un comentario en JavaScript

// Esta función muestra una alerta cuando se llama
function showAlert() {
    alert('¡Hola, Mundo!');
}

// Este código agrega un listener de eventos al objeto window
// Cuando la ventana se carga, llamará a la función showAlert
window.addEventListener('load', showAlert);
